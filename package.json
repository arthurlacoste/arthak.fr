{
  "name": "arthak-fr",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/local-cloud.mjs",
    "local:cloud": "node scripts/local-cloud.mjs"
  },
  "dependencies": {
    "marked": "^13.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
